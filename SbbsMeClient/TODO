Weibo
	api_key *=* "259989602"
	api_secret *=* "6144e89c9898bfada3fd3971b400d205"
	redirect_uri *=* "http://sbbs.me/weibo/callback"
	
provider :google_oauth2, '876356697116.apps.googleusercontent.com', 'Sc9hk9vgfb5nQCYCSG1niCoM', {}

上传一张图片
http://sbbs.me/api/upload_image
登录后可调用
POST参数：
    file=<上传的文件>
返回：成功返回服务器端的文件名（不需要完整路径），失败返回null

删除一张图片
http://sbbs.me/api/delete_image/<filename>
登录后可调用
filename参数为要删除的文件名，不带完整路径
返回：成功返回OK，其他返回均判为失败

获取用户上传的图片列表
http://sbbs.me/api/get_images
登录后可调用
返回：用户所属的图片列表(完整路径，创建时间等字段)，以json数组传回
    

2. about
3. 相册
4. 数据统计

create table sbbs_log(
	id int primary key auto_increment not null, 
	device text not null, 
	appver int not null, 
	osver text not null,
	email text, 
	action text not null, 
	detail text, 
	actiontime text not null
) character set utf8;

create table sbbs_feedback(
	id int primary key auto_increment, 
	device text, 
	appver int not null, 
	osver text not null, 
	email text, 
	userid text, 
	feedback text
) character set utf8;

select count(*) c from (select distinct device,osver,email from sbbs_log) a